{
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "cypress": "^13.13.1"
  },
  "scripts": {
    "dev": "cypress run --browser chrome --env version=\"dev\" --headed",
    "uat": "cypress run --browser chrome --env version=\"uat\" --headed"
  },
  "dependencies": {
    "cypress-plugin-api": "^2.11.2"
  }
}
